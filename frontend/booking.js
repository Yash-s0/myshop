// date_filed = cell.parentNode.cells["1"].innerHTML;
// console.log(date_filed)
// // alert("Date: " + date_filed)

// flight_class_field = cell.parentNode.cells["2"].innerHTML;
// console.log(flight_class_field)
// alert("Flight Class: " + flight_class_field);

// flight_from_field = cell.parentNode.cells["3"].innerHTML;
// console.log(flight_from_field)
// // alert("Flight From: " + flight_from_field);

// flight_id_field = cell.parentNode.cells["4"].innerHTML;
// console.log(flight_id_field)
// alert("Flight Id: " + flight_id_field);

// flight_to_field = cell.parentNode.cells["5"].innerHTML;
// console.log(flight_to_field)
// // alert("Flight To: " + flight_to_field);

// airline_filed = cell.outerText;
// console.log(airline_filed)
// // alert("Airline: " + airline_filed);

// price_filed = cell.parentNode.cells["6"].innerHTML;
// console.log(price_filed)
// // alert("Price: " + price_filed)

// var data = date_filed, flight_class_field, flight_from_field, flight_id_field, flight_to_field, airline_filed, price_filed

// let onloadFlaf = false;
// var flight_class_field
// window.onload = (flight_class_field) => {
    // console.log(flight_class_field)
    // if (onloadFlaf) {
    //     return;
    // }

    // onloadFlaf = true;
    // console.log(data)
    // Get the form element
    // var form = document.getElementById('myForm');

    // Get the form elements
    // var flight_from = form.elements.flight_from;
    // var flight_to = form.elements.flight_to;
    // var airline = form.elements.airline;
    // var price = form.elements.price;
    // var date = form.elements.date;
    // var flight_class = form.elements.flight_class;

    // Set the values
    // flight_from.value = "Delhi";
    // flight_to.value = data.flight_to_field;
    // airline.value = data.airline_filed;
    // price.value = data.price_filed;
    // date.value = data.date_filed;
    // flight_class.value = data.flight_class_field;
    // price.value = data.price_filed;
    // form.submit();
// }


